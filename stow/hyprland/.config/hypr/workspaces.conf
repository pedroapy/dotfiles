# ── Workspaces ─────────────────────────────────────
# Assign workspaces to monitors. Persistent makes the workspace
# always visible in waybar even when empty.
#
# Setup: HDMI-A-1 (primary, left) | DP-3 (right)

# Primary monitor (left) — workspaces 1..5
workspace = 1, monitor:HDMI-A-1, default:true, persistent:true
workspace = 2, monitor:HDMI-A-1, persistent:true
workspace = 3, monitor:HDMI-A-1, persistent:true
workspace = 4, monitor:HDMI-A-1, persistent:true
workspace = 5, monitor:HDMI-A-1, persistent:true

# Secondary monitor (right) — workspaces 6..10
workspace = 6, monitor:DP-3, default:true, persistent:true
workspace = 7, monitor:DP-3, persistent:true
workspace = 8, monitor:DP-3, persistent:true
workspace = 9, monitor:DP-3, persistent:true
workspace = 10, monitor:DP-3, persistent:true

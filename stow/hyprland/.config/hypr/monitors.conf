# ── Monitors ─────────────────────────────────────
# Format: monitor = name, resolution@rate, position, scale
# Use `hyprctl monitors` to list available monitors

# BenQ EW2780U 4K (primary, left)
monitor = DP-3, 3840x2160@60, 0x0, 1

# MSI G27CQ4 1440p (right)
monitor = HDMI-A-1, 2560x1440@60, 3840x0, 1

# Fallback for any other monitor
monitor = , preferred, auto, 1

# exec wlsunset -t 4500 -T 6500 -l 40.4167 -L -3.7037 -g 1.0
exec swaync -s /home/pyusta/.config/swaync/preset-0.css
exec nm-applet --indicator
exec blueman-applet
exec $HOME/.custom_script
exec wl-paste --type text --watch cliphist store
exec wl-paste --type image --watch cliphist store
exec swayidle -w timeout 300 'lock' timeout 600 'swaymsg "output * power off"' resume 'swaymsg "output * power on"' before-sleep 'lock'

exec nwg-panel -c pedro-config -s pedro-config.css
exec_always autotiling
